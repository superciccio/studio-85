import { CreditCardMaskDirective } from './credit-card-mask.directive';

describe('CreditCardMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCardMaskDirective();
    expect(directive).toBeTruthy();
  });
});
